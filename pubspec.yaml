name: flutter_throttling
description: contain "throttling" and "debouncing" async helper classes with listeners for flutter
version: 1.0.1
homepage: https://github.com/quantosapplications/throttling

environment:
  sdk: '>=2.12.0 <3.0.0'

dev_dependencies:
  test: ^1.15.3