name: throttling_example
description: Demonstrates how to use the "throttling" and "debouncing" async helper classes with listeners for flutter
version: 0.8.0
author: plugfox <plugfox@gmail.com>
homepage: https://github.com/PlugFox/throttling

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  throttling:
    path: ../

flutter:
